extends layout

block content
    h1 #{title}
    ul.list-group
        // loop into param
        each article, i in articles 
            li.list-group-item
                a(href="/article/"+article._id)= article.title
            