extends layout

block content
    h1 #{title}
    ul
        // loop into param
        each article, i in articles 
            li= article.title
            li= article.author
            li= article.body
            hr

block fotter
    h2 test footer block.